# Convex Configuration
# Get these values from https://dashboard.convex.dev
CONVEX_DEPLOYMENT=dev:your-deployment-name
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Frontend Configuration
# The URL where your frontend runs
VITE_SITE_URL=http://localhost:3001

# ==========================================
# Convex Environment Variables
# Set these using: npx convex env set VARIABLE_NAME value
# ==========================================

# Better Auth Secret (Convex Environment)
# Generate using: openssl rand -base64 32
# Set using: npx convex env set BETTER_AUTH_SECRET $(openssl rand -base64 32)
# BETTER_AUTH_SECRET=your-generated-secret

# Site URL (Convex Environment)
# Set using: npx convex env set SITE_URL http://localhost:3001
# For production: npx convex env set SITE_URL https://your-production-domain.com
# SITE_URL=http://localhost:3001

# Google OAuth (Convex Environment) - Optional
# Get these from: https://console.cloud.google.com/apis/credentials
# Set using:
#   npx convex env set GOOGLE_CLIENT_ID your_google_client_id
#   npx convex env set GOOGLE_CLIENT_SECRET your_google_client_secret
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
